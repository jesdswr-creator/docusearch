/* DocuSearch - Dark theme ("Midnight") override hooks.
   ⚠ SOURCE OF TRUTH: the authoritative master sheet is
   resources/themes/base.qss (compiled in as :/themes/base.qss), loaded
   by MainWindow::applyTheme(), which substitutes @token@ palette values
   and applies it via qApp->setStyleSheet(). Rules uncommented below are
   appended after that built-in sheet.
   To customise, mirror the selectors used in resources/themes/base.qss. */

/*
QMainWindow { background-color: #14181f; }
QPushButton { border-radius: 8px; }
*/
